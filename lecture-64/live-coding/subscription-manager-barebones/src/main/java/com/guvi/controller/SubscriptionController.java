package com.guvi.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * TODO: Implement Subscription endpoints.
 *
 * Suggested endpoints (baseline):
 * - POST /subscriptions               (subscribe to a plan)
 * - GET  /subscriptions/me            (current subscription + history)
 * - POST /subscriptions/{id}/cancel   (cancel active subscription)
 *
 * Baseline assumption:
 * - Use CurrentUserProvider (hard-coded user) for now.
 */
@RestController
@RequestMapping("/subscriptions")
public class SubscriptionController {
    // TODO: Add endpoints
}
