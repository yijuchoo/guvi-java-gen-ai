package com.guvi.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * TODO: Implement Plan endpoints.
 *
 * Suggested endpoints (baseline):
 * - GET /plans                (list active plans)
 * - GET /plans/{planCode}     (get plan by planCode)
 *
 * Note:
 * - Plans are seeded in memory by PlanSeeder.
 * - You can choose to add admin endpoints later.
 */
@RestController
@RequestMapping("/plans")
public class PlanController {
    // TODO: Add endpoints
}
